
# Red Hex behaviour control scripts:

## Setup:
Copy config_template to different location, outside of repo, and rename to config.json. Then replace the paths within it to match the correct paths for the computer that you are working on. 